@model List<Fiby.Cloud.Web.DTO.Modules.Reportes.Response.ReporteSemanaDTOResponse>

<div class="table-responsive">
    <table id="tbReporteLogin" class="table table-striped table-bordered">
        <thead id="thReporteLoginData">
            <tr>
                <th class="text-center">Empleado</th>
                <th class="text-center">Usuario</th>
                <th class="text-center">Fecha Ult Ingreso</th>
            </tr>
        </thead>
        <tbody id="tbReporteLoginData">
            @for (var i = 0; i < Model.Count(); i++)
            {
                var item = Model.ToList()[i];

                <tr>
                    <td style="text-align:center;">@item.Cliente</td>
                    <td style="text-align:center;">@item.Nombres</td>
                    <td style="text-align:center;">@item.Fecha</td>
                </tr>
            }
        </tbody>
    </table>
</div>